# PR Review Log #3

## Summary
- **Scope**: Add `task_id <= 0` validation and standardize null-check style (`is None`) in `get_task`, `update_task`, and `delete_task`.
- **Risk**: Medium — merge conflict with `main`; potential regression of existing business logic (done-state guard, `list_tasks_by_status` endpoint).
- **Positives**:
  - Explicit `is None` check is more correct than truthy check.
  - Input validation for invalid IDs is a good defensive practice.
- **Concerns**:
  1. **Does not achieve stated goal**: Title says "Refactor: duplicated search logic" but the code still contains 3 identical copies of the same block — duplication is *standardized* but not *eliminated*. A proper refactor would extract a shared dependency.
  2. **Merge conflict / regression risk**: Branch is stale relative to `main`. Missing `TaskStatus` import, `list_tasks_by_status` endpoint, and the "Cannot modify a completed task" guard that exists on `main`.
  3. **No tests**: The `tests/test_tasks.py` file on this branch is empty. New validation behavior (400 on `task_id <= 0`) is untested.
  4. **Redundant comments**: Inline comments duplicate the function-level documentation.

## Decision
**Request changes.**

## Next steps
1. Rebase onto current `main` and resolve merge conflicts (preserve `list_tasks_by_status` endpoint and done-state guard).
2. Extract the repeated validation+fetch block into a shared FastAPI dependency (`get_task_or_404`) to actually eliminate the duplication.
3. Add tests covering the new 400 response for `task_id <= 0`.
4. Remove redundant inline comments.
